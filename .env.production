# Production Environment Configuration
# このファイルは本番環境（Cloud Run）用の環境変数設定です
# Secret Manager経由で管理される機密情報（OAuth認証情報、JWT秘密鍵）はここには含まれません

# 環境設定
ENVIRONMENT=production
DEBUG=false
LOG_LEVEL=INFO
LOG_FORMAT=json

# Secret Manager設定
SECRET_MANAGER_ENABLED=true
GCP_PROJECT_ID=interview-api-472500
OAUTH_CREDENTIALS_SECRET_NAME=google-oauth-credentials
JWT_KEY_SECRET_NAME=jwt-secret-key

# Firestore設定
FIREBASE_ENABLED=true
USE_LOCAL_CONFIG=false

# 許可ホスト（Cloud RunのURL）
ALLOWED_HOSTS=unified-auth-server-856773980753.asia-northeast1.run.app,localhost:8000

# ドメイン設定
ALLOWED_DOMAINS=i-seifu.jp,i-seifu.ac.jp

# CORS設定
CORS_ORIGINS=http://localhost:3000,http://localhost:8501,http://localhost:8000
CORS_ALLOW_CREDENTIALS=true

# JWT設定
JWT_ALGORITHM=HS256
JWT_EXPIRY_DAYS=30

# API Proxy設定
API_PROXY_SERVER_URL=https://api-key-server-856773980753.asia-northeast1.run.app
API_PROXY_CLIENT_ID=unified-auth-server

# 注意事項：
# - GOOGLE_CLIENT_ID, GOOGLE_CLIENT_SECRET, JWT_SECRET_KEY は
#   Secret Manager経由で管理されるため、ここには記載しません
# - 機密情報を含む環境変数は絶対にこのファイルに書かないでください
# - このファイルはGitにコミットされます（機密情報なし）
